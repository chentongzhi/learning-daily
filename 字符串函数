#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
#include<string.h>
#include<ctype.h>

int main()
{
	//char ch = '2';
	//int ret = islower(ch);
	//int ret = isdigit(ch);
	//printf("%d\n", ret);

	//char ch = tolower('q');
	//char ch = toupper('q');
	//putchar(ch);

	char arr[] = "I Am A Student";
	int i = 0;
	while (arr[i])
	{
		if (isupper(arr[i]))
		{
			arr[i] = tolower(arr[i]);

		}
		i++;
	}
	return 0;
}
